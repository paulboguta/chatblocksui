---
title: AI Input
description: Displays a text input with a button to send messages to an AI.
---

import { ComponentDocs } from '@/components/preview/component-docs';
import { TypeTable } from 'fumadocs-ui/components/type-table';

<ComponentDocs 
  name="ai-input"
  packageName="example"
  cliDependencies={[]}
  manualDependencies={[]}
  examples={[]}
/>

## API Reference

### AI Input

<TypeTable
  type={{
    placeholder: {
      description: 'Placeholder text for the input field',
      type: 'string',
      default: '"Ask me anything..."',
    },
    onSubmit: {
      description: 'Callback function when the send button is clicked',
      type: '(message: string) => void',
      default: 'undefined',
    },
    disabled: {
      description: 'Whether the input is disabled',
      type: 'boolean',
      default: 'false',
    },
  }}
/>